{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "playwright install chromium && playwright install-deps chromium && python cron_job.py",
    "restartPolicyType": "NEVER"
  },
  "cron": {
    "schedule": "0 * * * *"
  }
}
